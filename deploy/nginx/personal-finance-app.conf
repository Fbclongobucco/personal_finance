server {
    listen 80;
    listen [::]:80;
    server_name api-personal-finance.fabriciolongobuccodev.com.br;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# HTTPS block, disabled until a certificate exists.
#
# Get a certificate first (either run certbot's nginx plugin, which edits this
# file itself, or issue one standalone/manually and uncomment below):
#   sudo certbot certonly --nginx -d api-personal-finance.fabriciolongobuccodev.com.br
#
# Then uncomment this block, remove the "location /" above from the :80
# server (replace it with the redirect below) and reload nginx.
#
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name api-personal-finance.fabriciolongobuccodev.com.br;
#
#     ssl_certificate     /etc/letsencrypt/live/api-personal-finance.fabriciolongobuccodev.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api-personal-finance.fabriciolongobuccodev.com.br/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
#
# server {
#     listen 80;
#     listen [::]:80;
#     server_name api-personal-finance.fabriciolongobuccodev.com.br;
#     return 301 https://$host$request_uri;
# }
