spring:
  datasource:
    url: ${APP_DB_URL:jdbc:postgresql://localhost:5432/personal_finance_app}
    username: ${APP_DB_USERNAME:postgres}
    password: ${APP_DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  h2:
    console:
      enabled: false
